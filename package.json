{
  "name": "ipadresstrackerjs",
  "version": "1.0.0",
  "description": "Проект \"Определение геолокации по IP\"",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html  --out-dir=build",
    "predeploy": "rm -rf dist && parcel build index.html --public-url IPAdressTrackerJS",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "leaflet": "^1.9.4"
  }
}
